# Multimodal Math Mentor - Environment Variables
# Copy this file to .env and fill in your actual values

# ============================================
# API Keys
# ============================================

# Groq API Key (Required)
# Get your API key from: https://console.groq.com/
# Sign up for free and get your API key from the API Keys section
GROQ_API_KEY=your_groq_api_key_here

# Optional: OpenAI API Key (for backward compatibility)
# Only needed if you want to use OpenAI models instead of Groq
# OPENAI_API_KEY=your_openai_api_key_here

# ============================================
# Model Configuration (Optional)
# ============================================

# LLM Model (default: llama-3.1-8b-instant)
# Other options: mixtral-8x7b-32768, gemma-7b-it
# DEFAULT_LLM_MODEL=llama-3.1-8b-instant

# Embedding Model (default: all-MiniLM-L6-v2)
# EMBEDDING_MODEL=all-MiniLM-L6-v2

# ============================================
# RAG Configuration (Optional)
# ============================================

# Number of chunks to retrieve (default: 5)
# RAG_TOP_K=5

# Chunk size for text splitting (default: 500)
# CHUNK_SIZE=500

# Chunk overlap (default: 50)
# CHUNK_OVERLAP=50

# ============================================
# Confidence Thresholds (Optional)
# ============================================

# OCR confidence threshold (default: 0.7)
# Triggers HITL if OCR confidence is below this value
# OCR_CONFIDENCE_THRESHOLD=0.7

# ASR confidence threshold (default: 0.7)
# Triggers HITL if ASR confidence is below this value
# ASR_CONFIDENCE_THRESHOLD=0.7

# Verifier confidence threshold (default: 0.8)
# Triggers HITL if verifier confidence is below this value
# VERIFIER_CONFIDENCE_THRESHOLD=0.8

# ============================================
# Paths (Optional - usually don't need to change)
# ============================================

# Knowledge base directory (default: knowledge_base)
# KNOWLEDGE_BASE_DIR=knowledge_base

# Memory database path (default: memory_db/math_mentor_memory.db)
# MEMORY_DB_PATH=memory_db/math_mentor_memory.db

# Vector store path (default: vector_store)
# VECTOR_STORE_PATH=vector_store

# ============================================
# Setup Instructions
# ============================================
#
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Edit .env and add your GROQ_API_KEY:
#    GROQ_API_KEY=gsk_your_actual_api_key_here
#
# 3. Get your API key from:
#    https://console.groq.com/
#
# 4. The .env file is in .gitignore, so it won't be committed to git
#
# ============================================
